def credit_check(num):
    #split the num into individual numbers
    num_list = list(map(int,str(num)))
    #multiple every other digit starting at the first one
    
    #take the numbers that are doubled, seperate them into indiv numbers and add them
    #add all the numbers up and if the result % 10 = 0, its valid


credit_check(5541808923795240)


